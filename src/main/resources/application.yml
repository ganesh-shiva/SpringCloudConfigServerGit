server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ganesh-shiva/ConfigServerTest
          #username: 
          #password: 
          search-paths:
          - app*
          force-pull: true